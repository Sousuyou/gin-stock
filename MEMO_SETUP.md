# スタッフメモ機能 セットアップ（1回だけ・約5分）

各ジンの詳細画面に「スタッフメモ」を表示・投稿・編集・削除できる機能です。
**全員が読めて、スタッフPINを入れた人なら誰でも書き換えられる**設定になっています。

申請箱（新規ジン申請）と同じ Supabase プロジェクトを使うので、追加の準備はSQLを1回流すだけです。

## 手順

1. Supabase のダッシュボードを開く（申請箱で使っているプロジェクト）。
2. 左メニューの **SQL Editor** を開く。
3. このフォルダの **`supabase_memos_setup.sql`** の中身をすべてコピーして貼り付け、**Run** を押す。
4. 左メニューの **Table Editor** に `gin_memos` というテーブルが増えていればOK。
   - 鍵マーク（RLS enabled）が付いていることを確認。
   - Policies が「anon insert memo」「anon read active memo」「anon update active memo」「anon delete active memo」の4つになっていることを確認。

これで、カタログでジンをタップ → 詳細画面の下に「スタッフメモ」欄が出ます。
**「＋ メモを追加（スタッフ）」→ PIN入力**で書き込み・編集・削除できます（PINは申請ページと同じ。既定 `soutsu2026`）。

## 既にメモテーブルを作成済みの場合

編集・削除を有効にするだけなら、短い `supabase_memos_edit_permissions.sql` だけをSQL EditorでRunしてもOKです。

## 運用メモ

- **いたずら書き・不要メモを消したいとき**：画面側の「削除」で消せます。Table Editor で その行の `status` を `hidden` に変えても、すぐ画面から消えます。
- メモは「公開（全員が読める）」前提です。お客さんに見せたくない内容は書かないでください。
- SQLを流していない間は、メモ欄は「まだメモはありません」と出るだけで、カタログは普通に動きます。
