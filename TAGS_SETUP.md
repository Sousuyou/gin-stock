# 風味タグ機能 セットアップ（1回だけ・約3分）

各ジンに「フローラル」「シトラス」などの**風味タグ**を、スタッフが付け外しできる機能です。
**全員が見られて、スタッフPINを入れた人だけが編集できます**（メモ機能と同じ作り）。

## 手順

1. ⚠️ **必ず「正しいプロジェクト」を開く** … `gin_submissions`（申請箱）や `gin_memos`（メモ）があるプロジェクトです。Table Editor にそれらが見えるのが目印。
2. 左メニュー **「SQL Editor」** → **「＋ New query」**
3. このフォルダの **`supabase_tags_setup.sql`** の中身を全部コピーして貼り付け、**Run**
4. 「Success. No rows returned」と出ればOK
5. Table Editor に **`gin_flavor_tags`** が増えていれば完了（鍵マーク＝RLS有効／Policiesが3つ）

> SQLの最後に `NOTIFY pgrst, 'reload schema';` を入れてあるので、APIへの反映もこの1回で済みます。

## 使い方
カタログでジンをタップ → 詳細の下に **「風味タグ」**。
**「＋ タグを付ける（スタッフ）」→ PIN**（メモと同じ・既定 `soutsu2026`）→ 候補をタップで追加。
解錠後は、付与済みタグの **×** で取り消しできます。
付いたタグは全員に見え、ツールバーの **「風味タグ」** で絞り込めます。

## 運用メモ
- **間違って付けたタグ**：スタッフPINで解錠し、タグ横の **×** で取り消せます。Table Editor → `gin_flavor_tags` → その行の `status` を `hidden` にしても即消えます。
- 同じジンに同じタグが二重に入っても、画面では重複を除いて1つだけ表示します。
- SQL未実行の間は、タグ欄は「まだタグはありません」と出るだけで、カタログは普通に動きます。
