# ジン在庫カタログ｜自動更新の設定手順

スプレッドシートを編集すると、カタログが自動で最新になる仕組みです。
**鍵・パスワードは使いません。費用も無料です。**

---

## ⚠️ 一番大事な前提

自動更新をONにすると、**Googleスプレッドシートが「正（マスター）」**になります。
カタログは「スプレッドシートに書いてある通り」を映すだけになります。

→ なので**まず、今回ボタニカル等を補完した整形版を、あなたのスプレッドシートとして採用**してください。
これをやらずに古い表のまま公開すると、せっかく埋めた中身が空欄に戻ってしまいます。

---

## Part 1. 整形版を「これからの在庫表」にする（1回だけ）

1. お渡しした `BarSoustu_ジン在庫管理表_整形版_20260608.xlsx` を Google ドライブに**ドラッグ＆ドロップ**
2. アップロードされたファイルを**右クリック → アプリで開く → Google スプレッドシート**
3. これを今後の在庫管理表として使う（古い表は消さず残してOK）
4. 以後、銘柄の追加・修正は**このシート**で行う

## Part 2. シートを「ウェブに公開（CSV）」する

1. シートを開いて **ファイル → 共有 → ウェブに公開**
2. 「リンク」タブで、公開範囲＝**「在庫管理」シート**、形式＝**カンマ区切り形式（.csv）** を選ぶ
   - ※「ドキュメント全体」ではなく**シート単体**を選ぶこと（他に見せたくないタブがあっても安全）
3. **「公開」**を押す → 表示された **URL をコピー**
   - URLは `https://docs.google.com/spreadsheets/d/e/2PACX-…/pub?gid=…&single=true&output=csv` の形

## Part 3. GitHub に置いて Pages を有効化

1. `gin-stock-repo` フォルダを GitHub のリポジトリにする（git push、または GitHub Desktop でドラッグ）
2. リポジトリ内の **`sheet_source.txt`** を開き、**Part 2 でコピーしたURLを貼って保存・コミット**
3. リポジトリの **Settings → Pages** で、ブランチ `main` / フォルダ `/ (root)` を選んで保存
   → 数分で `https://（ユーザー名）.github.io/gin-stock/` で公開される
4. 共通ナビに載せる場合は `top/nav.js` の LINKS に1行追加（任意・別チャット担当）

## Part 4. 自動更新を動かす

- **毎日自動**で更新されます（`/.github/workflows/update-stock.yml` の設定。日本時間あさ6時ごろ）
- **すぐ反映したいとき**：GitHubの **Actions タブ → 「在庫データ自動更新」→ Run workflow** を押す
- 更新頻度を変えたいときは yml の `cron:` を編集（例：6時間ごとなら `0 */6 * * *`）

---

## これで日々の運用は

1. スプレッドシートで銘柄を**追加・修正**する
2. 何もしなくても、翌朝（or 手動実行で即）カタログに反映される

## 仕組みと安全性のメモ

- GitHub Actions が、公開CSVを読んで `gins.json` を作り直し、自動でコミットするだけ。
- **認証情報（鍵・パスワード）は一切不要** → 漏れる秘密がない。
- 公開されるのは「在庫管理」タブ＝カタログの中身そのものなので、**追加で漏れる情報はゼロ**。
- GitHubアカウントは全ツール共通の急所 → **2段階認証(2FA)を必ず有効に**。

## 私（Claude）が代行できない部分

GitHubリポジトリの作成・Pages有効化・Googleシートの公開は、**あなたのGoogle/GitHubアカウント**が必要なため私には実行できません。上記手順でお願いします（詰まったら画面を見せてくれれば一緒に進めます）。
